import { makeStyles } from "@material-ui/core/styles";
const styles = makeStyles((theme) => ({
  gridContainer: {
    padding: "1rem",
  },
  gridItem: {
    padding: "1rem",
  },
}));

export default styles;
